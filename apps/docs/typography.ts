import { type PluginUtils } from "tailwindcss/types/config";

export default function typographyStyles({}: PluginUtils) {
  return {
    DEFAULT: {
      css: {

      }
    },
    invert: {
      css: {
        
      }
    }
  }
}